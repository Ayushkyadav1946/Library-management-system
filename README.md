# Library-management-system
The **Library Management System** is a Java-based program that runs through the command line. It is designed to make it easier for library staff to manage books, members, and loans. The main purpose of the project is to show how data structures and algorithms can be used to handle common problems in a library
